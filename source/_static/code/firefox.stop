#!/bin/bash

# COPY THE CONFIGURATION FILE FROM MEMORY TO $HOME

#set -x

TEMP=`mktemp --tmpdir=/home/liuhui -d`
mv /tmp/tmp.*.firefox $TEMP && mv $TEMP /home/liuhui/.mozilla-backup
rm -f /home/liuhui/.mozilla

#set +x
